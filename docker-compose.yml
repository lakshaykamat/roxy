services:
  roxy:
    build:
      context: .
    env_file:
      - .env
    environment:
      DATABASE_PATH: /app/data/roxy.db
    ports:
      - "8888:8888"
    volumes:
      - roxy_data:/app/data
    init: true
    restart: unless-stopped

volumes:
  roxy_data:
